{
  "main": "build/src/index.js",
  "devDependencies": {
    "@google-cloud/pubsub": "^1.4.1",
    "@susisu/type-of-schema": "^0.2.0",
    "@types/node": "^10.0.3",
    "@types/node-fetch": "^2.5.4",
    "gts": "^1.1.2",
    "typescript": "~3.7.0"
  },
  "scripts": {
    "check": "gts check",
    "clean": "gts clean",
    "compile": "tsc -p .",
    "fix": "gts fix",
    "prepare": "yarn run compile",
    "pretest": "yarn run compile",
    "posttest": "yarn run check"
  },
  "dependencies": {
    "ajv": "^6.12.3",
    "node-fetch": "^2.6.0"
  },
  "private": true,
  "license": "UNLICENSED"
}
